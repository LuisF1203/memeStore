<script>


</script>

<div class="w-full py-6 px-3">
    <div class="">
        <a href="{{ product_product.url }}">
            {% if product_product.featured_image %}
                <div class=" overflow-hidden relative">
                    {% if product_product.compare_at_price_max > product_product.price %}
                        <div class="absolute z-10 bg-red-500 text-white p-2 rounded-xl top-5 left-5 font-bold">
                            {{ product_product.compare_at_price_max | minus: product_product.price | times: 100.0 | divided_by: product_product.compare_at_price_max | money_without_currency | times: 100 | remove: '.0'}}% OFF
                        </div>
                    {% endif %}

                    <img  onmouseleave="this.src='{{ product_product.images[0] | image_url: width: 960 }}'" onmouseenter="this.src='{{ product_product.images[1] | image_url: width: 960 }}'" class="w-full h-full object-cover transition duration-1000 ease-in-out hover:scale-105 transform"
                        src="{{ product_product.images[0] | image_url: width: 960 }}"
                        width="{{ product_product.images[0].width }}"
                        height="{{ product_product.images[0].height }}"
                        loading="lazy"
                        alt="{{ product_product.images[0].alt }}"
                    >
                </div>
                <p class="text-center mt-2">{{ product_product.title }}</p>
                <p class="text-center mt-2 font-bold">{{ product_product.price | money }}</p>
                <p class="text-center line-through opacity-50">{{ product_product.compare_at_price | money  }}</p>



                
            {% else %}
                {{ 'product-1' | placeholder_svg_tag: 'w-full h-full object-cover bg-gray-100' }}
            {% endif %}
        </a>
    </div>
</div>

<style>
    @media (max-width: 768px) {
        .shopNowLink{
            font-size: 1rem;
            padding: 0;
        }
    }

</style>